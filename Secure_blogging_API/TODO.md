# Secure Blogging API - Connect to Server Task

## Steps:
- [x] Step 1: Create .env with MongoDB URI and other vars
- [x] Step 2: Install dependencies (npm install)
- [x] Step 3: Start server (npm run dev)
- [x] Step 4: Verify DB connection and health check

## Progress: 4/4 completed

**Note:** Update .env with valid MONGO_URI (MongoDB Atlas) and JWT_SECRET before running. Kill port 5000 if needed (y to install kill-port), then 'rs' in nodemon terminal. Server ready on http://localhost:5000/api/v1/health

